Konfiguracja Apache-a oraz Nginx-a
===================================

Apache
------

Yii jest gotowe do pracy z domyœln¹ konfiguracj¹ serwera Apache. Pliki .htaccess w folderach frameworku oraz aplikacji ograniczaj¹ dostêp do odpowiednich zasobów. W celu ukrycia pliku rozruchowego (zazwyczaj index.php) w adresie URL, mo¿esz dodaæ nastêpuj¹ce instrukcje mod_rewrite do pliku .htaccess w g³ównym katalogu lub konfiguracji wirtualnego hosta:

~~~
RewriteEngine on

# jeœli katalog lub plik istniej¹, u¿yj ich 
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# w przecwinym przypadku przeka¿ do index.php
RewriteRule . index.php
~~~


Nginx
-----

Mo¿esz u¿ywaæ Yii z [Nginx-em](http://wiki.nginx.org/) i PHP z [FPM SAPI](http://php.net/install.fpm).
Poni¿ej znajduje siê przyk³adowa konfiguracja hosta. Definiuje ona plik rozruchowy i pozwala yii przechwytywaæ ¿¹dania prowadz¹ce do nieistniej¹cych plików, co pozwala nam posiadaæ ³adnie wygl¹daj¹ce URLe.

~~~
server {
    set $host_path "/www/mysite";
    access_log  /www/mysite/log/access.log  main;

    server_name  mysite;
    root   $host_path/htdocs;
    set $yii_bootstrap "index.php";

    charset utf-8;

    location / {
        index  index.html $yii_bootstrap;
        try_files $uri $uri/ $yii_bootstrap?$args;
    }

    location ~ ^/(protected|framework|nbproject|themes/\w+/views) {
        deny  all;
    }

    #unikaj przetwarzania przez Yii wywo³añ do nieistniej¹cych statycznych plików 
    location ~ \.(js|css|png|jpg|gif|swf|ico|pdf|mov|fla|zip|rar)$ {
        try_files $uri =404;
    }

    # przeka¿ skrypty PHP do serwera FastCGI nas³uchuj¹cego 127.0.0.1:9000
    #
    location ~ \.php {
        fastcgi_split_path_info  ^(.+\.php)(.*)$;

        #let yii catch the calls to unexising PHP files
        set $fsn /$yii_bootstrap;
        if (-f $document_root$fastcgi_script_name){
            set $fsn $fastcgi_script_name;
        }

        fastcgi_pass   127.0.0.1:9000;
        include fastcgi_params;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fsn;

        #PATH_INFO and PATH_TRANSLATED can be omitted, but RFC 3875 specifies them for CGI
        fastcgi_param  PATH_INFO        $fastcgi_path_info;
        fastcgi_param  PATH_TRANSLATED  $document_root$fsn;
    }

    location ~ /\.ht {
        deny  all;
    }
}
~~~
U¿ywaj¹c tej konfiguracji mo¿esz ustawiæ cgi.fix_pathinfo=0 w pliku php.ini w celu unikniêcia wielu niepotrzebnych wywo³añ systemowych stat().

<div class="revision">$Id: quickstart.apache-nginx-config.txt 3512 2011-12-27 16:50:03Z haertl.mike $</div>