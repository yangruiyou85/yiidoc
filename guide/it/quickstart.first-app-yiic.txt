Generazione di Codice usando gli Strumenti di riga di comando (sconsigliato)
=========================================

> Nota: I generatori di codice della `shell yiic` sono sconsigliati sin dalla versione 1.1.2. 
Si consiglia invece di utilizzare i generatori di codice "Web-based" più potenti e flessibili che sono disponibili qui: [Gii](/doc/guide/topics.gii).

Apri una finestra di comando, ed esegui i comandi elencati di seguito,

~~~
% cd WebRoot/testdrive
% protected/yiic shell
Yii Interactive Tool v1.1
Please type 'help' for help. Type 'exit' to quit.
>> model User tbl_user
   generate models/User.php
   generate fixtures/tbl_user.php
   generate unit/UserTest.php

The following model classes are successfully generated:
    User

If you have a 'db' database connection, you can test these models now with:
    $model=User::model()->find();
    print_r($model);

>> crud User
   generate UserController.php
   generate UserTest.php
   mkdir D:/testdrive/protected/views/user
   generate create.php
   generate update.php
   generate index.php
   generate view.php
   generate admin.php
   generate _form.php
   generate _view.php

Crud 'user' has been successfully created. You may access it via:
http://hostname/path/to/index.php?r=user
~~~

Qui sopra è usato il comando `yiic shell` per interagire con la nostra applicazione.
Al prompt eseguiamo due sotto-comandi: `model User tbl_user` 
e `crud User`. Il primo genera a classe di modello chiamata `User` per la tabella `tbl_user`,
mentre il secondo analizza il modello `User` e genera il codice implementando
le corrispondenti operazioni CRUD. 

> Nota: Potresti riscontrare errori tipo "...could not find driver", sebbene
> il controllo requisiti riporti che hai abilitato il PDO
> e il driver PDO  corrispondente. Se ciò accade, puoi provare
> a lanciare lo strumento `yiic`come segue,
>
> ~~~
> % php -c path/to/php.ini protected/yiic.php shell
> ~~~
>
> where `path/to/php.ini` represents the correct PHP ini file.

Puoi vedere il nostro lavoro visitando il seguente:

~~~
http://hostname/testdrive/index.php?r=user
~~~

Ti verr� mostrato una lista di registrazione utenti della tabella`tbl_user`.

Clicca sul pulsante `Create User` nella pagina. Se non ti sei gi� loggato in precedenza 
sarai indirizzato alla pagina di login. Dopo il log in, potrai
vedere un form che ti permetter� di aggiungere un nuovo utente. Completa il form
e clicca sul pulsante `Create`. Se c'� un errore di input, ti sar� mostrato 
un messaggio di errore che serve a prevenire il salvataggio. Tornando all'elenco utenti registrati,
dovresti vedere l'utente appena aggiunto nella lista.

Ripeti i passi sopra esposti per aggiungere pi� utenti. Nota che la pagina della lista utenti
verr� automaticamente impaginata se ci sono troppi utenti da essere visualizzati
in una pagina sola.

Se fai il login come amministratore usando `admin/admin`, puoi vedere la pagina utente
admin con il seguente URL:

~~~
http://hostname/testdrive/index.php?r=user/admin
~~~

Questo ti mostrer� le registrazioni utente in un bel formato tabella. Puoi cliccare
sull'intestazione delle celle per ordinare le corrrispondenti colonne. Puoi cliccare sui bottoni
di ciascuna riga di dati per vedere, aggiornare o cancellare la riga di dati corrispondente.
Puoi navigare tra pagine differenti. Puoi anche filtrare e cercare i dati a cui sei interessato.

Tutte queste ottime caratteristiche sono disponibili senza che sia necessario
scrivere una sola riga di codice!

![User admin page](first-app6.png)

![Create new user page](first-app7.png)



<div class="revision">$Id$</div>